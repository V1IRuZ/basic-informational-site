# Basic Informational Site

This project tested NodeJS and Express to create a server. The server created with NodeJS alone was set to port 8080, while the Express version was set to 3000.

The project contains four pages: `index`, `about`, `contact-me` and `404`. These were each routed to their own address and `404` for invalid urls.