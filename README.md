# Basic Informational Site

This project tested NodeJS to create a server on local port 8080.

The project contains four pages: `index`, `about`, `contact-me` and `404`. These were each routed to their own address and `404` for invalid urls.